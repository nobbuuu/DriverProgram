package com.haylion.android.data.util;

public final class TimeUtil {

/*
    public final static SimpleDateFormat DEFAULT_SDF;
    public final static SimpleDateFormat DEFAULT_DATE_SDF;
    public final static SimpleDateFormat DEFAULT_TIME_SDF;

    static {
        DEFAULT_SDF = new SimpleDateFormat("yyyy-MM-dd HH:mm:ss", Locale.getDefault());
        DEFAULT_DATE_SDF = new SimpleDateFormat("yyyy-MM-dd", Locale.getDefault());
        DEFAULT_TIME_SDF = new SimpleDateFormat("HH:mm:ss", Locale.getDefault());
    }

    private TimeUtil() {
        throw new UnsupportedOperationException("hey, buddy, easy...");
    }

    public static String getCurrTimeString() {
        return getCurrTimeString(DEFAULT_SDF);
    }

    public static String getCurrTimeString(SimpleDateFormat format) {
        return date2String(new Date(), format);
    }

    public static String getDateString() {
        return getDateString(DEFAULT_DATE_SDF);
    }

    public static String getDateString(SimpleDateFormat format) {
        return date2String(new Date(), format);
    }

    public static long getCurrTimeMills() {
        return System.currentTimeMillis();
    }

    public static String formatTimestamp(long timestamp) {
        return formatTimestamp(timestamp, DEFAULT_SDF);
    }

    public static String formatTimestamp(long timestamp, SimpleDateFormat format) {
        return date2String(new Date(timestamp), format);
    }

    public static String date2String(Date time, SimpleDateFormat format) {
        return format.format(time);
    }

    public static int numberOfDaysInMonth(int month, int year) {
        Calendar monthStart = new GregorianCalendar(year, month, 1);
        return monthStart.getActualMaximum(Calendar.DAY_OF_MONTH);
    }

    public static int numberOfDaysInMonth() {
        Calendar calendar = Calendar.getInstance();
        return numberOfDaysInMonth(Calendar.MONTH, Calendar.YEAR);
    }
*/

}
